// Re-export component
export { default } from "./Typography"

// Re-export types (both individual and namespace)
export { default as TypographyTypes } from "./types"
export type { TypographyProps, TypographyModeType, AlignType, AsType } from "./types"
