/**
 * Determines the effective color to use
 *
 * Returns the custom color value provided by the user.
 *
 * @param color - User-provided custom color (supports CSS colors, variables, etc.)
 * @returns Effective color value or undefined
 */
const getEffectiveColor = (
  color: string | undefined
): string | undefined => {
  return color
}

export default getEffectiveColor