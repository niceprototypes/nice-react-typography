[2026-05-24 06:00] major: Drop the no-op `TypographyStyles` ComponentType export. Token CSS is provided by build-time `nice-styles/tokens.css` (loaded by `StylesProvider`); `TypographyStyles` rendered nothing and is removed from `src/index.ts`, `src/tokens/index.ts`, and `src/tokens/TypographyStyles.ts` (file deleted). Consumers using `<StylesProvider componentStyles={[TypographyStyles, …]}>` should drop the prop entirely (also removed in nice-react-styles@major).
[2026-05-23 21:05] patch: Remove broken `test: jest` and `lint: eslint` scripts from package.json — neither tool had a config file. Priority 1.5 normalization.
[2026-05-23 20:07] patch: Drop unused `rollup-plugin-peer-deps-external` devDependency — externalization is now handled by `nice-configuration/rollup`'s built-in peerDependencies reader. No build-output change.
[2026-05-20 15:08] major: `mode` prop now wraps the rendered element in `<Mode name={mode}>` instead of pinning the styled-component to that mode in isolation. Behavior change: descendants of a mode-pinned Typography now inherit the pin via the data-theme cascade (previously they did not). Styled-components no longer thread `$mode` internally — semantic tokens resolve via cascade. Children that set their own `mode` continue to override.
[2026-05-19 12:00] patch: Update consumer call sites to the unified getToken from nice-react-styles (was getReactToken). Internal-only rename — Typography's public props are unchanged. Comments updated to reference nice-styles' renamed CSS artifact (tokens.css).
